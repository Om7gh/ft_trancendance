// function findConversation(convDb, userId, targetId) {
// 	let result = convDb.find((conv) => {
// 		if ( (conv.user1.id === userId && conv.user2.id === targetId)
// 			|| (conv.user1.id === targetId && conv.user2.id === userId) )
// 			return (true);
// 		return (false);
// 	});
	
// 	return (result);
// }

// export default findConversation;